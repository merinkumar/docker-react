sudo: required
services:
    - docker

before_install:
    - docker build -t merinkumar/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true merinkumar/docker-react npm run test -- --coverage